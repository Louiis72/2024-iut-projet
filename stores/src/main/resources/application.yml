
spring:
  application.name: stores

  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    #url: jdbc:h2:mem:testdb
    url: jdbc:h2:file:./data/storedb
    driverClassName: org.h2.Driver
    username: sa
    password: louis
  h2:
    console:
      enabled: true

server:
  port: 8082